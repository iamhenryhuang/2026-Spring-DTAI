services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - WEIGHTS_DIR=/app/weights
    volumes:
      - model-weights:/app/weights

volumes:
  model-weights:
